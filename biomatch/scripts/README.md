# Scripts

- `ingest_person.py`: ajout d’une personne en base avec calcul des empreintes

Exemple:

```bash
python scripts/ingest_person.py \
  --name "Jean Dupont" \
  --email "jean.dupont@example.com" \
  --image "C:/path/photo.jpg" \
  --video "C:/path/video.mp4"
```